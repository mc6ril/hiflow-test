import en from '@/shared/i18n/messages/en.json';

import type { TranslationDictionary } from './types';

const messagesEn: TranslationDictionary = Object.freeze(en);

export const resources = Object.freeze({
  en: messagesEn,
});
