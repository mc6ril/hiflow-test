import fr from '@/shared/i18n/messages/fr.json';

import type { TranslationDictionary } from './types';

export const resources = Object.freeze({
  fr: Object.freeze(fr) as TranslationDictionary,
});
