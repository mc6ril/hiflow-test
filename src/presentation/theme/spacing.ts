import { normalize } from '@/presentation/utils/normalize';

export const spacing = Object.freeze({
  xs: normalize(4),
  sm: normalize(8),
  md: normalize(12),
  lg: normalize(16),
  xl: normalize(24),
  xxl: normalize(32),
});
